\subsection{Úvod} 
	\noindent 
		Prechod na postkvantovú kryptografiu so sebou prináša aj potrebu implementova podpisové
		schémy pomocou postkvantového kryptosystému. Vo všeobecnosti sa na realizáciu digitálneho
		podpisu vyuívajú asymetrické kryptosystémy, respektíve kryptosystémy s verejnım k¾úèom.
		Kryptosystémy, ktoré sme si predstavili v predchádzajúcej èasti spåòajú toto kritérium.
		Všeobecná schéma na vytvorenie digitálneho podpisu správy má pod¾a definície tieto èasti
		
	\begin{description}
		\item[$\bullet$] Algoritmus na generovanie páru privátnych a verejnıch k¾úèov
		\item[$\bullet$] Podpisovı algoritmus závislı od privátneho k¾úèa, ktorı vytvorí podpis pre danú správu
		\item[$\bullet$] Overovací algoritmus závislı od verejného k¾úèa, ktorı pre správu prijme alebo zamietne zodpovedajúci podpis		
	\end{description}
	
	Niektoré kryptosystémy túto schému implementujú tak, e ako podpisovú funkciu pouijú dešifrovací algoritmus s prívatnım k¾úèom a 
	ako overovaciu zvolia šifrovací algoritmus s verejnım k¾úèom. Podpis a overenie v tejto implementácii môe vyzera takto 
	
	\begin{algorithm}[h]
		\lstset{
			basicstyle=\small\sffamily,
			frame=none,
			numbers=left,
			xleftmargin=5.0ex,
			numberstyle=\tiny,
			stepnumber=1,
			showstringspaces=false,
			keywordstyle=\color{blue}\bfseries
			}
		\lstset{emph={%  Adjust any special keywords
			printf%
			},emphstyle={\color[rgb]{1,0,0}\bfseries}%
			}%
		\begin{lstlisting}
Máme správu m, ktorú chceme podpísa a odtlaèkovú funkciu H
Vypoèítame odtlaèok h = H(m)
Vypoèítame podpis tak, e dešifrujeme odtlaèok h pomocou privátneho k¾úèa
Podpísanú správu tvorí dvojica m a s
		\end{lstlisting}
		\caption{Schéma digitálneho podpisu}
		\label{digital-signature}
	\end{algorithm}

	Monos tejto implmentácie je silne závisla od toho, ako sa
	prekrıvajú mnoiny šifrovanıch textov a odtlaèkov v konkrétnom kryptosystéme. Ako si ukáeme v ïalších èastiach práce,
	nie všetky odtlaèky musia by dešifrovate¾né správy.

\subsection{Preh¾ad code-based podpisovıch schém}
	\noindent
		V ïalších èastiach práce sa u budeme zaobera iba code-based podpisovımi schémami, teda schémami, ktoré vyuívajú
		code-based kryptosystémy. Ve¾kou prekákou tıchto kryptosystémov je v súèasnosti ve¾kos k¾úèa, ktorá je v porovnaní s 
		dnešnımi kryptosystémami rádovo tisícnásobne väèšia. Pri implementácii a následne v praxi je dôleité nájs vhodnı kompromis
		medzi poadovanou bezpeènosou a vıpoètovou a dátovou nároènosou, ktorá zavisí od vo¾by ve¾kosti k¾úèa.
	\newline
		Existuje nieko¾ko potenciálnych návrhov code-based kryptosystémov, z ktorıch si blišie prejdeme CFS (Courtois-Finiasz-Sendrier) a LDGM (Low-density generator matrix).
		
\subsection{CFS schéma}
	\noindent 
		Jednım z nádejnıch návrhov code-based podpisovıch schém je CFS schéma, ktorá pouíva
		na podpisovanie Niederreiterov kryptosystém.
		Základnı problém, ktorı treba vyrieši pri podpisovaní zaloenom na kódovaní, je ako získa takı
		odtlaèok správy, ktorı je dekódovate¾né slovo. Ak máme lineárny kód $C(n,k,2t+1)$, syndróm slova je vektor dåky $n-k$.
		Poèet všetkıch syndrómov je $2^{n-k}$ a poèet dekódovate¾nıch syndrómov je 
	\newline 
		$\sum_{i=0}^{t}$ $n \choose i$ .
		To znamená, e $\dfrac{{\sum_{i=0}^{t} {n \choose i}}}{2^{n-k}}$ všetkıch syndrómov je dekódovate¾nıch. Pre Goppove kódy je to pribline $\dfrac{1}{t!}$.
		Pravdepodobnos, e odtlaèok správy bude zároveò dekódovate¾nı, je teda pribline $p = \dfrac{1}{t!}$. Nato, aby sme vedeli podpísa kadú správu, budeme
		musie ku správe prida bity navyše, a pokúsi sa podpísa túto upravenú správu. Priemernı poèet pokusov na podpísanie jednej správy je pribline $t!$.
	\newline
	\begin{algorithm}[h]
		\lstset{
			basicstyle=\small\sffamily,
			frame=none,
			numbers=left,
			xleftmargin=5.0ex,
			numberstyle=\tiny,
			stepnumber=1,
			showstringspaces=false,
			keywordstyle=\color{blue}\bfseries
			}
		\lstset{emph={%  Adjust any special keywords
			printf%
			},emphstyle={\color[rgb]{1,0,0}\bfseries}%
			}%
		\begin{lstlisting}
Vstup: Správa m, odtlaèková funkcia H ktorá vracia hashe dåky n-k (dåka syndrómu)
Zvolíme i = 0, h = H(m || i)
Pokia¾ h nie je dekódovate¾né, zvolíme i = i + 1 a opakujeme predošlı krok
e = D(s)
signature = (e, i)
		\end{lstlisting}
		\caption{Algoritmus podpisovania v CFS}
		\label{CFS-digital-signature}
	\end{algorithm}
	
	\noindent 
		Implementácia uvedeného algoritmu môe by vylepšená po viacerıch stránkach. Prvé vylepšenie sa dá realizova pri h¾adaní dekódovate¾ného syndrómu. 
		Na zaèiatku podpisovania si vypoèítame hash samotnej správy $h' = H(m)$ a v ïa¾ších krokoch poèítame $h = H(h' || i)$. Ïalší priestor na vylepšenie, tentokrát
		dåka vısledného podpisu, sa ponúka v spôsobe uloenia èasti $e$ z podpisu. Autori tejto podpisovej schémy navrhli uklada $e$ ako index $I$ z mnoiny všetkıch $n$ bitovıch
		vektorov s váhou $t$. To predstavuje èíslo z rozsahu $<1, {n \choose t}>$
		
	\begin{algorithm}[ht]
		\lstset{
			basicstyle=\small\sffamily,
			frame=none,
			numbers=left,
			xleftmargin=5.0ex,
			numberstyle=\tiny,
			stepnumber=1,
			showstringspaces=false,
			keywordstyle=\color{blue}\bfseries
			}
		\lstset{emph={%  Adjust any special keywords
			printf%
			},emphstyle={\color[rgb]{1,0,0}\bfseries}%
			}%
		\begin{lstlisting}
Vstup: podpis (e, i), správa m, verejnı k¾úè H', odtlaèková funkcia H ktorá vracia hashe dåky n-k (dåka syndrómu)

Vypoèítame s1 = H*e^t
Vypoèítame s2 = H(m || i)

Ak s1 = s2, podpis príjimame, inak zamietame
		\end{lstlisting}
		\caption{Algoritmus overovania v CFS}
		\label{CFS-digital-signature-verify}
		
	\noindent
		
	\end{algorithm}
	
	\subsubsection{Praktické parametre}
	\subsubsection{CFS s pôvodnım McEliece systémom}
\subsection{LDGM podpis}
	\noindent
		Ïalší z monıch návrhov pre code-based kryptografiu sa pokúša zmenši potrebnú ve¾kos k¾úèa pomocou vhodne zvoleného
		kódu, respektíve pomocou vhodne zvolenej generaènej matice. LDGM (Low-density generation matrix) kódy, èie kódy s riedkou generaènou maticou
		sa v niektorıch prípadoch dajú zapísa kompaktne pomocou cirkulantnıch matíc. Generaèná matica $G$ kódu dåky $n$ s dimenziou $k$ sa skladá
		z $k_{0}n_{0}$ blokov s rozmermi $p\times p$, kde $n_{0}=n/p$ a $k_{0}=k/p$. 
		
		\begin{center} $ G = \begin{pmatrix}
						  C_{0,0} & C_{0,1} & \cdots & C_{0,n_{0}-1} \\
						  C_{1,0} & C_{1,1} & \cdots & C_{1,n_{0}-1} \\  
						  \vdots & \vdots & \ddots & \cdots \\  
						  C_{k_{0}-1,0} & C_{k_{0}-1,1} & \cdots & C_{k_{0}-1,n_{0}-1} \\
							\end{pmatrix}$
		\: \: \: 
		\end{center}
		
		Kadé $C_{i,j}$ je $p \times p$ cirkulatná matica. Vïaka tomu nám staèí uloi z kadého bloku iba jeden riadok. Tım zmenšíme ve¾kos klúèa
		$p$-násobne. Matica v takomto tvare sa nazıva kvázicyklická (QC). K matici $G$ vypoèítame kontrolnú maticu $H$ v systematickom tvare, t.j. $H = [X | I]$.
		Kontrolná matica $H$ je súèasou súkromného k¾úèa. Ïalšiu èas k¾úèa tvoria matice $Q$ a $S$.
		Postup ako urèi maticu $Q$ je zhrnutı v nasledovnom algoritme.
		
	\begin{algorithm}[ht]
		\lstset{
			basicstyle=\small\sffamily,
			frame=none,
			numbers=left,
			xleftmargin=5.0ex,
			numberstyle=\tiny,
			stepnumber=1,
			showstringspaces=false,
			keywordstyle=\color{blue}\bfseries
			}
		\lstset{emph={%  Adjust any special keywords
			printf%
			},emphstyle={\color[rgb]{1,0,0}\bfseries}%
			}%
		\begin{lstlisting}
Urèíme náhodne matice a, b s rozmermi z x r, z <= r (r = n-k)
Vypoèítame maticu R = trans(a)*b X ones(p,p)   //ones - matica p x p same jednotky, X Kroneckerov sucin
Urèíme maticu T poskladanú z r0*r0 cirkulantnıch matic tak, aby váha kadého riadku aj ståpca bola wt a aby rank(R+T) = r
Q = R + T			
		\end{lstlisting}
		\caption{Vıpoèet matice Q}
		\label{matrix-Q}
		
	\noindent
		
	\end{algorithm}
	
	\noindent
		Maticu $S$ urèíme ako náhodnú maticu poskladanú z $r_0 \times r_0$ cirkulantnıch blokov ve¾kosti $p \times p$ tak, aby 
		váha kadého riadku aj ståpca bola $w_s$ a aby mala plnú hodnos.
	
	\noindent
		Verejnı k¾úè tvorí upravená kontrolná matica $H = Q^{-1}HS^{-1}$. Spôsob, ktorım poèítame matice $Q$ a $S$ zachovávajú QC vlastnosti pôvodnej matice $H$,
		èo nám umonuje zmenšenie ve¾kosti verejného k¾úèa.
	  
	  
\subsection{Porovnanie}